# Environment configuration file - defines the current IDC and environment tags to test
# Default configuration: local test environment

# Current IDC to test (local_test, aws_offline, gcp_offline, aws_online, gcp_online)
idc: "local_test"

# Current environment tag to test
env: "local"

# Server language configuration (currently supports: python)
# Used to determine coverage collection methods and other language-specific features
server_language: "python"
