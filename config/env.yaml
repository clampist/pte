# 环境配置文件 - 定义当前要测试的IDC和环境标签
# 默认配置：本地测试环境

# 当前要测试的IDC (local_test, aws_offline, gcp_offline, aws_online, gcp_online)
idc: "local_test"

# 当前要测试的环境标签
env: "local"

# 服务器语言配置 (目前支持: python)
# 用于确定覆盖率收集方式和其他语言特定功能
server_language: "python"
